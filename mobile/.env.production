# Production API — used by CI (Deploy Frontend workflow) and when building for production.
# Must match your backend production URL (e.g. Vercel backend).
API_BASE_URL=https://helixacareai.vercel.app
